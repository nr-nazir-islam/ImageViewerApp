<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.imageviewer">

    <!-- শুধু ইন্টারনেট — এটা auto-granted, কোনো prompt দেখায় না -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ⚠️ লক্ষ্য করুন: ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION নেই -->
    <!-- ইউজারকে কিছু চাইতে হবে না -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Photo Gallery"
        android:supportsRtl="true"
        android:theme="@style/Theme.ImageViewerApp"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>